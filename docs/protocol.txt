# Protocol
## Architecture decisions
- Pure HttpServer (JDK) for REST; no Spring.
- Layers: config, db, http, model, repo, service, util.

## Technical steps
- Init DB via `db/init/001_schema.sql` (users table).
- Implemented register/login (token issued, stored).
- Password hashing with `PasswordUtil`.

## Unit tests & what/why
- (Describe what you test or note “N/A yet, planned for …”)

## Problems & solutions
- Example: Cross-platform line endings → `.gitattributes`.
- Postgres container healthcheck timing → added wait/retry, etc.

## Time tracking (estimate)
- Setup & DB: 2h
- HTTP endpoints: 3h
- Auth/token: 2h
- Docs & Postman: 1h

> Note: git history in the repo is part of the documentation.
